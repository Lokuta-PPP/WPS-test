name: 测试推送到WPS

on:
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: 发送测试消息
        env:
          WPS_KEY: ${{ secrets.WPS_WEBHOOK_KEY }}
        run: |
          curl "https://qyapi.wps.cn/cgi-bin/webhook/send?key=${WPS_KEY}" \
            -H "Content-Type: application/json" \
            -d '{"msgtype": "text", "text": {"content": "🎉 测试消息：GitHub Actions 连接成功！"}}'
