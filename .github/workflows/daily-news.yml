name: 测试推送到WPS第二次

on:
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: 发送测试消息
        env:
          WPS_URL: ${{ secrets.WPS_WEBHOOK_URL }}
        run: |
          curl "${WPS_URL}" \
            -H "Content-Type: application/json" \
            -d '{"msgtype": "text", "text": {"content": "🎉 测试成功！"}}'
